---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import { getCollection } from "astro:content";
const allBlogPosts = await getCollection("blog");

const page = {
  title: "Blog",
  meta: {
    description: "This is where I will post about my journey learning Astro.",
  },
};
---

<Layout page={page}>
  <main>
    <Header />
    <article class="prose max-w-full dark:prose-invert">
      <h1>hi, i'm Harold</h1>
      <h4>i catch bugs for a living</h4>
      {
        allBlogPosts.map((p) => (
          <a href={`/haroldbjc-work/blog/${p.slug}`}>{p.data.title}</a>
        ))
      }
    </article>
  </main>
</Layout>

<style>
  main {
    margin: auto;
    padding: 1rem;
    width: 800px;
    max-width: calc(100% - 2rem);
    font-size: 20px;
    line-height: 1.6;
  }
</style>

<style>
  main {
    margin: auto;
    padding: 1rem;
    width: 800px;
    max-width: calc(100% - 2rem);
    font-size: 20px;
    line-height: 1.6;
  }
</style>
